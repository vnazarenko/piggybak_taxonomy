<%= render :partial => "breadcrumb", :locals => { :final_node => @node, :link_last => false } %>

<% if @node.sellables.any? -%>
<%= render :partial => "sellable", :collection => @node.sellables %>
<% else -%>
There are no products.
<% end -%>
